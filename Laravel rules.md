# Laravel 后端开发规范（企业通用版）

本文档适用于所有基于 **Laravel 8/9/10/11** 的后端项目，统一团队编码、目录、接口、数据库、安全、部署标准，解决 Laravel 开发常见的松散、不统一、不规范问题，兼顾框架特性与工程化规范。

**核心原则**：充分利用 Laravel 优雅特性、代码简洁可复用、分层职责单一、杜绝原生 PHP 野蛮写法、所有业务可追溯、安全无漏洞。

## 一、项目目录与分层规范（强制）

遵循 **控制器薄、业务厚、模型干净** 原则，禁止控制器堆积业务逻辑。

### 1\. 核心目录职责

- **app/Http/Controllers**：控制器，只做「接收请求\+参数校验\+调用服务\+返回响应」，不写任何业务逻辑、SQL 查询

- **app/Services**：自定义业务服务层（Laravel 无默认目录，必须新建），存放核心业务逻辑、事务、复杂计算，所有控制器业务统一下沉至此

- **app/Models**：模型，只做「数据表映射、关联关系、访问器/修改器、模型作用域」，不写复杂业务

- **app/Repositories**（可选）：数据仓库层，复杂查询、多表查询统一封装，控制器/服务不直接写复杂 SQL

- **app/Http/Requests**：表单验证类，所有接口入参校验统一存放，杜绝控制器内手动校验参数

- **app/Http/Resources**：资源响应类，统一格式化返回前端数据，隐藏敏感字段

- **app/Enums**：枚举类，存放所有业务状态、类型常量，消灭魔法数字

- **app/Exceptions**：自定义异常、全局异常处理器

- **app/Helpers**：全局工具函数

- **app/Configs**：自定义业务配置

### 2\. 禁止写法

- 禁止控制器内写 DB::query 原生查询、循环逻辑、事务操作

- 禁止模型内写业务判断、接口响应、日志输出

- 禁止把所有逻辑堆在控制器，形成“大控制器”烂代码

## 二、命名规范（统一强制）

### 1\. 文件与目录

- 目录：小写蛇形命名，如 `app/Services/Order/`

- 类文件：大驼峰命名，如 `OrderService.php`、`UserRequest.php`

### 2\. 代码命名

- 类名：大驼峰 `UserController、OrderService`

- 方法名：小驼峰，动词开头 `getUserInfo、createOrder、cancelOrder`

- 变量名：小驼峰，语义清晰，禁止 `$a、$b、$temp`

- 常量：全大写下划线`ORDER_STATUS_PAID`

- 枚举值：全大写下划线

### 3\. 路由命名

- 路由 URI：全小写、横杠分隔，禁止下划线、驼峰 `/api/user-info`

- 路由别名：蛇形命名 `user.info`

## 三、路由规范

### 1\. 版本控制

所有接口必须做版本隔离，避免迭代冲突，统一路由前缀：

- v1 版本：`/api/v1/xxx`

- 后台接口统一前缀：`/api/v1/admin/xxx`

- 前台接口统一前缀：`/api/v1/front/xxx`

### 2\. RESTful 规范（强制）

- **GET**：查询数据（列表、详情），不修改数据

- **POST**：新增数据

- **PUT**：全量更新数据

- **PATCH**：局部更新数据

- **DELETE**：删除数据（优先逻辑删除）

### 3\. 路由约束

- 禁止定义动词路由：`/get-user、/add-order`

- 资源路由优先使用 `Route::resource`，精简代码

- 所有路由必须绑定中间件：登录校验、权限校验、跨域、限流

## 四、控制器规范

### 1\. 控制器职责单一

控制器只做三件事，**不写任何业务逻辑**：

1. 接收 HTTP 请求

2. 调用 Request 做参数校验

3. 调用 Service 业务方法，统一返回响应

### 2\. 代码示例（标准写法）

```php
public function store(UserRequest $request)
{
    $data = $request->validated();
    $user = UserService::instance()->createUser($data);
    return $this->success($user);
}

```

### 3\. 强制约束

- 禁止在控制器写 `DB::select/insert/update`

- 禁止控制器内 if/else 嵌套业务判断

- 禁止手动 `try/catch` 捕获业务异常，统一交给全局异常处理器

## 五、参数校验规范（Request 类）

**所有接口入参必须使用独立 Request 验证类，禁止控制器内 validate 校验**。

### 规范要求

- 新增、修改、查询接口拆分独立 Request，不混用

- 必填、类型、长度、范围、手机号、邮箱等规则严格校验

- 自定义错误提示，不使用系统默认英文提示

- 使用 `authorize()` 方法做接口权限校验

## 六、Service 业务层规范

Laravel 无原生 Service 层，是业务统一收口的核心，所有复杂逻辑必须下沉至此。

### 1\. 规范要求

- 单一职责：一个 Service 对应一个业务模块（用户、订单、支付）

- 事务统一写在 Service 层，使用 `DB::transaction()`

- 禁止跨模块直接调用私有方法，公共方法对外提供能力

- 业务判断、状态校验、幂等判断全部放在 Service

### 2\. 事务强制规范

- 多表写入、数据更新、资金操作**必须加事务**

- 事务粒度最小化，禁止大事务、长事务

## 七、Model 模型规范

### 1\. 基础配置（强制）

- 表名：数据库蛇形小写，模型自动适配，禁止手动绑定错误表名

- 主键：统一 `id`，自增 bigint

- 统一开启时间戳：`$timestamps = true`

- 统一逻辑删除：使用 Laravel 自带 `SoftDeletes`，禁止物理删除业务数据

### 2\. 模型禁止写法

- 禁止在模型写业务逻辑、日志、响应

- 禁止滥用全局作用域导致数据查询异常

- 禁止隐藏必要字段、随意修改访问器导致数据错乱

### 3\. 模型推荐用法

- 关联关系（hasOne/hasMany/belongsTo）统一写在模型

- 字段格式化使用访问器/修改器

- 固定查询条件使用局部作用域

## 八、枚举 Enum 规范（强制消灭魔法数字）

所有状态、类型、标识位**禁止使用 0/1/2 魔法数字**，统一使用枚举管理。

### 规范要求

- 新建 `app/Enums` 目录，按业务模块拆分枚举

- 枚举包含 code（数据库存储值）、desc（中文描述）

- 必须提供 `getByCode` 静态方法，实现数据库值反向匹配枚举

- 接口判断、状态赋值、列表展示全部使用枚举，杜绝硬编码

## 九、数据库规范

### 1\. 数据表设计

- 表名、字段名：小写蛇形，禁止驼峰、大写

- 所有表必备字段：`id、created_at、updated_at、deleted_at`

- 禁止无主键表、无时间字段表

- 字段注释完整，清晰说明用途

### 2\. SQL 编写规范

- 禁止 `select *`，明确查询所需字段

- 禁止原生 SQL 字符串拼接，使用 Laravel 查询构造器、参数绑定，防止 SQL 注入

- 批量数据必须分页，禁止全表查询

- update/delete 必须带 where 条件，生产环境禁止无条件更新删除

- 关联查询优先使用模型关联，少写原生 join

### 3\. 索引规范

- where、join、order by、group by 高频字段建立索引

- 禁止建立无用索引、过多索引影响写入性能

- 避免索引失效：like 左模糊、字段函数运算、隐式类型转换

## 十、统一响应格式规范

全局统一接口返回结构，禁止五花八门的返回格式。

### 标准返回结构

```json
{
    "code": 200,
    "msg": "操作成功",
    "data": {},
    "timestamp": 1725000000
}

```

### 状态码约定

- 200：业务成功

- 400：参数错误

- 401：未登录/Token过期

- 403：权限不足

- 404：资源不存在

- 500：服务器业务异常

通过 **全局响应助手方法** 统一返回：`success() / fail()`

## 十一、异常与日志规范

### 1\. 异常处理

- 禁止业务代码裸奔报错，统一自定义业务异常

- 禁止随意 try/catch 吞掉异常，必须抛出或记录日志

- 所有异常统一由 `app/Exceptions/Handler.php` 全局处理

- 前端只展示友好提示，不暴露数据库、代码堆栈信息

### 2\. 日志规范

- 使用 Laravel 自带 Log 门面，禁止 `var_dump、echo、print_r` 调试遗留代码

- 关键业务节点必须打日志：入参、操作人、订单ID、执行结果

- 错误日志必须携带异常堆栈`Log::error('错误信息', [$e])`

- 禁止打印密码、Token、手机号、身份证等敏感数据

## 十二、安全开发规范（重点）

- **参数过滤**：所有接口入参必校验，不信任前端任何数据

- **防SQL注入**：杜绝原生SQL拼接，全部使用参数绑定

- **权限控制**：所有接口必须登录校验\+权限校验，无后门接口

- **密码安全**：密码使用 `bcrypt` 加密存储，禁止明文、MD5简单加密

- **敏感数据脱敏**：手机号、身份证、银行卡返回前端必须脱敏

- **防重放/防重复提交**：新增、支付、扣款接口增加防重机制

- **文件上传安全**：校验文件后缀、MIME类型、限制大小、禁止执行脚本文件

- **环境配置安全**：密钥、数据库密码、Token密钥存放 \.env，禁止硬编码提交Git

## 十三、Git 与版本规范

### 1\. 分支规范

- main：生产稳定分支，禁止直接提交

- test：测试环境分支

- dev：开发主分支

- feature/xxx：功能开发分支

- fix/xxx：bug修复分支

### 2\. Commit 规范

- feat：新增功能

- fix：修复bug

- refactor：代码重构，无功能变更

- optimize：性能/代码优化

- docs：文档注释更新

禁止提交：调试代码、打印代码、密钥、本地配置文件、废弃注释代码

## 十四、Laravel 专属禁忌（绝对禁止）

1. 滥用 `DB::raw()` 原生SQL，导致注入风险

2. 控制器堆积大量业务、SQL、判断逻辑

3. 接口返回格式不统一，混用数组、对象、字符串

4. 数据库无条件 update/delete、全表查询

5. 代码硬编码 0/1/2 状态值，不使用枚举

6. \.env 敏感信息提交到代码仓库

7. 生产环境开启 debug 模式

8. 废弃代码注释留存、大量冗余代码不清理

9. 复杂业务无事务，导致数据不一致

## 十五、补充最佳实践

- 分页统一使用 Laravel 自带 `paginate()`，统一返回分页结构

- 列表查询使用模型作用域封装通用筛选逻辑

- 频繁查询数据使用 Redis 缓存，统一缓存Key命名规范

- 定时任务统一在 `Kernel.php` 配置，禁止手动脚本执行

- 核心业务编写单元测试，保证迭代稳定性

- 接口文档实时维护（Swagger/ApiFox），与代码同步更新

> （注：部分内容可能由 AI 生成）
